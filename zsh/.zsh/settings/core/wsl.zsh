############################################################
# WSL向け設定
############################################################

# Windows側のパスを優先しない
export PATH=$(echo "$PATH" | tr ':' '\n' | grep -v '/mnt/c/Windows/System32' | tr '\n' ':')

# less を見やすく
export LESS='-R'
